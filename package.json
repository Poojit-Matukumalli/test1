{
  "name": "vite-node-static-ci",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && vite build --ssr src/entry-server.js --outDir dist/server",
    "preview": "vite preview",
    "start": "node server.js",
    "test": "npm run build && node test-static.js"
  },
  "devDependencies": {
    "vite": "^5.4.1"
  }
}
